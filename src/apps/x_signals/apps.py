"""X Signals app configuration."""

from django.apps import AppConfig


class XSignalsConfig(AppConfig):
    """X Signals app config."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.x_signals"
